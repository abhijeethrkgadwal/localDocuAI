# Production origin used for canonical URLs, sitemap, Open Graph, and llms.txt.
# No trailing slash. Do not invent a domain — set the real production origin.
VITE_SITE_URL=https://localdocu.app

# Optional overrides (defaults are set in siteConfig.ts).
# VITE_LINKEDIN_URL=https://www.linkedin.com/in/abhijeethrkgadwal/
# VITE_PORTFOLIO_URL=https://www.davnix.com/abhijeeth-gadwal

# Optional. Leave empty until sponsorship/funding is active (avoids dead links).
# VITE_SUPPORT_URL=
